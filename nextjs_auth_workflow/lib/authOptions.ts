// NextAuth Configuration (authOptions)
// This file will contain authentication providers (Google, GitHub, etc.)
// and callbacks used by NextAuth.js.

export const authOptions = {
  // TODO: Add providers, callbacks, pages, etc.
};
