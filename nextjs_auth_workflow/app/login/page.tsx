// Login Page
// This page will render the login form (e.g., <LoginForm /> component).
// Users will be redirected here if they are not authenticated.

export default function LoginPage() {
  return (
    <div>
      <h2>Login</h2>
      {/* TODO: Insert <LoginForm /> component here */}
    </div>
  );
}
