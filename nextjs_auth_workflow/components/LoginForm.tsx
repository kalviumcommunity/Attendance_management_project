// LoginForm Component
// This is the UI for user login.
// Will eventually contain input fields and buttons for login (email/password, OAuth, etc.).
// For now, it's just a placeholder.

export default function LoginForm() {
  return (
    <form>
      <p>// TODO: Add login inputs and sign-in button</p>
    </form>
  );
}
